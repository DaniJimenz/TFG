{# templates/_navbar.html.twig #}
<nav class="fixed top-0 left-0 right-0 z-50 bg-[#101c22]/80 backdrop-blur-xl border-b border-primary/20">
    <div class="max-w-[1400px] mx-auto px-6 h-20 flex items-center justify-between">

        <a href="{{ path('app_home') }}" class="flex items-center gap-3 group shrink-0">
            <h1 class="text-2xl font-black italic uppercase text-white tracking-tighter">
                META<span class="text-primary">FIT</span> AI
            </h1>
            <div class="bg-primary p-1.5 rounded-lg shadow-[0_0_15px_rgba(13,166,242,0.4)] group-hover:scale-110 transition-transform">
                <span class="material-symbols-outlined text-white text-xl">bolt</span>
            </div>
        </a>

        <div class="hidden lg:flex items-center gap-8">
            <a href="{{ path('app_home') }}" class="group flex items-center gap-2 text-[#90b7cb] hover:text-white font-black text-[11px] uppercase tracking-widest transition-all">
                <span class="material-symbols-outlined text-lg group-hover:text-primary transition-colors">dashboard</span>
                Panel
            </a>

            <a href="#" class="group flex items-center gap-2 text-[#90b7cb] hover:text-white font-black text-[11px] uppercase tracking-widest transition-all">
                <span class="material-symbols-outlined text-lg group-hover:text-primary transition-colors">menu_book</span>
                Biblioteca
            </a>

            <a href="#" class="group flex items-center gap-2 text-[#90b7cb] hover:text-white font-black text-[11px] uppercase tracking-widest transition-all">
                <span class="material-symbols-outlined text-lg group-hover:text-primary transition-colors">fitness_center</span>
                Mi Rutina
            </a>

            <a href="#" class="group flex items-center gap-2 text-[#90b7cb] hover:text-white font-black text-[11px] uppercase tracking-widest transition-all">
                <span class="material-symbols-outlined text-lg group-hover:text-primary transition-colors">monitoring</span>
                Progreso
            </a>

            <a href="#" class="flex items-center gap-2 bg-primary/10 hover:bg-primary/20 text-primary px-4 py-2 rounded-full border border-primary/30 font-black text-[11px] uppercase tracking-widest transition-all shadow-[0_0_15px_rgba(13,166,242,0.1)]">
                <span class="material-symbols-outlined text-lg">auto_awesome</span>
                Escanear IA
            </a>
        </div>

        <div class="flex items-center gap-5 shrink-0">
            <div class="hidden sm:flex flex-col items-end border-r border-slate-700 pr-5">
                <span class="text-white font-black text-[10px] uppercase tracking-tighter mb-1">{{ app.user.name }}</span>
                <span class="text-primary text-[9px] font-black uppercase tracking-[0.2em]">LVL {{ app.user.level }}</span>
            </div>

            <a href="{{ path('app_logout') }}" class="group flex items-center justify-center bg-red-500/5 hover:bg-red-500/20 text-red-500/70 hover:text-red-500 w-10 h-10 rounded-xl transition-all border border-red-500/10 hover:border-red-500/30">
                <span class="material-symbols-outlined text-xl">logout</span>
            </a>
        </div>
    </div>
</nav>
