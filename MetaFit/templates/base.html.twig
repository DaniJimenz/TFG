<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}MetaFit AI{% endblock %}</title>

    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {% block stylesheets %}
        <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
        <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet"/>
        <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>

        <script>
            tailwind.config = {
                darkMode: "class",
                theme: {
                    extend: {
                        colors: {
                            "primary": "#0d59f2",
                            "accent-purple": "#8b5cf6",
                            "background-dark": "#0a0c12",
                        },
                        fontFamily: { "display": ["Space Grotesk", "sans-serif"] }
                    },
                },
            }
        </script>
        <style type="text/tailwindcss">
            @layer base {
                body { font-family: 'Space Grotesk', sans-serif; }
            }
            .bg-mesh {
                background-color: #0a0c12;
                background-image:
                    radial-gradient(at 0% 0%, rgba(13, 89, 242, 0.12) 0px, transparent 40%),
                    radial-gradient(at 100% 100%, rgba(139, 92, 246, 0.08) 0px, transparent 40%);
            }
            /* Ocultar scrollbar pero permitir scroll con rueda del ratón */
            .scrollbar-hide::-webkit-scrollbar {
                display: none;
            }
            .scrollbar-hide {
                -ms-overflow-style: none;
                scrollbar-width: none;
            }
        </style>
    {% endblock %}

    {% block javascripts %}
        {{ importmap('app') }}
    {% endblock %}
</head>
<body class="bg-background-dark text-slate-200 font-display min-h-screen bg-mesh overflow-hidden">

{% set show_ui = app.user and app.request.attributes.get('_route') != 'app_onboarding' %}

<div class="flex h-screen w-screen overflow-hidden">

    {# --- SIDEBAR (Para todos los usuarios) --- #}
    {% if show_ui %}
        {# OVERLAY BACKDROP PARA MÓVIL #}
        <div id="sidebar-backdrop" class="fixed inset-0 bg-black/80 z-50 hidden md:hidden backdrop-blur-sm transition-opacity duration-300 opacity-0" onclick="toggleSidebar()"></div>

        <aside id="sidebar" class="w-64 bg-[#0d1117] border-r border-slate-800/50 flex flex-col shrink-0 z-[60] fixed md:relative h-full -translate-x-full md:translate-x-0 transition-transform duration-300 ease-in-out">
            <div class="p-6 flex flex-col h-full">
                {# Logo MetaFit AI - Clickeable al dashboard (admin o usuario) #}
                <a href="{% if is_granted('ROLE_ADMIN') %}{{ path('admin_dashboard') }}{% else %}{{ path('app_home') }}{% endif %}" class="flex items-center gap-3 mb-10 px-2 hover:opacity-80 transition-opacity">
                    <h1 class="text-xl font-black italic uppercase text-white tracking-tighter">
                        META<span class="text-primary">FIT</span>
                    </h1>
                </a>

                {# Enlaces de navegación #}
                <nav class="space-y-2 flex-1">
                    {% if is_granted('ROLE_ADMIN') %}
                        {# MENÚ ADMIN #}
                        <a href="{{ path('admin_dashboard') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl {% if 'admin_dashboard' in app.request.attributes.get('_route') %}bg-red-900/30 text-red-500 border border-red-500/20{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %} transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">dashboard</span>
                            Dashboard
                        </a>
                        <a href="{{ path('admin_users_index') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl {% if 'admin_users_' in app.request.attributes.get('_route') %}bg-red-900/30 text-red-500 border border-red-500/20{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %} transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">manage_accounts</span>
                            Usuarios
                        </a>
                        <a href="{{ path('admin_exercises_index') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl {% if 'admin_exercises_' in app.request.attributes.get('_route') %}bg-red-900/30 text-red-500 border border-red-500/20{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %} transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">edit</span>
                            Ejercicios
                        </a>
                        <a href="{{ path('admin_stats_index') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl {% if 'admin_stats_' in app.request.attributes.get('_route') %}bg-red-900/30 text-red-500 border border-red-500/20{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %} transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">analytics</span>
                            Estadísticas
                        </a>
                    {% else %}
                        {# MENÚ USUARIO NORMAL #}
                        <a href="{{ path('meal_index') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl {% if 'meal_' in app.request.attributes.get('_route') %}bg-primary/10 text-primary border border-primary/20{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %} transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">nutrition</span>
                            Nutrición
                        </a>
                        <a href="{{ path('training_index') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl {% if 'training_' in app.request.attributes.get('_route') %}bg-primary/10 text-primary border border-primary/20{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %} transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">fitness_center</span>
                            Entrenamientos
                        </a>
                        <a href="{{ path('routine_index') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl {% if 'routine_' in app.request.attributes.get('_route') %}bg-primary/10 text-primary border border-primary/20{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %} transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">repeat</span>
                            Mis Rutinas
                        </a>
                        <a href="{{ path('app_library_index') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl text-slate-400 hover:bg-slate-800/50 hover:text-white transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">library_books</span>
                            Biblioteca
                        </a>
                        <a href="{{ path('achievement_index') }}" class="flex items-center gap-4 px-4 py-3 rounded-xl {% if 'achievement_' in app.request.attributes.get('_route') %}bg-primary/10 text-primary border border-primary/20{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %} transition-all font-bold text-sm tracking-widest uppercase">
                            <span class="material-symbols-outlined">trophy</span>
                            Logros
                        </a>
                    {% endif %}
                </nav>

                {# Usuario Inferior #}
                <div class="pt-6 border-t border-slate-800/50">
                    <div class="flex items-center gap-3 p-2">
                        <div class="w-10 h-10 rounded-full bg-slate-800 flex items-center justify-center font-bold {% if is_granted('ROLE_ADMIN') %}text-red-400 bg-red-900/30{% else %}text-primary{% endif %}">
                            {{ app.user.name|first|upper }}
                        </div>
                        <div class="flex-1 min-w-0">
                            <p class="text-sm font-bold truncate leading-none">{{ app.user.name }}</p>
                            <p class="text-[10px] {% if is_granted('ROLE_ADMIN') %}text-red-400 font-bold{% else %}text-primary font-bold{% endif %} uppercase mt-1">{% if is_granted('ROLE_ADMIN') %}Admin{% else %}User{% endif %}</p>
                        </div>
                    </div>
                </div>
            </div>
        </aside>
    {% endif %}

    {# --- CONTENIDO DERECHA (Top Nav + Bloque Body) --- #}
    <div class="flex-1 flex flex-col min-w-0">

        {# Navbar Superior (Estadísticas y Logout) #}
        {% if show_ui %}
            <header class="bg-[#0d1117]/50 backdrop-blur-md border-b border-slate-800/50 flex items-center px-4 sm:px-6 py-2 shrink-0 sticky top-0 z-40">
                <div class="flex items-center justify-between w-full gap-2 sm:gap-4">
                    {# Botón menú móvil y espacio #}
                    <div class="flex-1 flex items-center">
                        <button onclick="toggleSidebar()" class="md:hidden p-1.5 sm:p-2 rounded-lg bg-slate-800/40 border border-slate-700/50 text-slate-400 hover:text-white transition-all mr-3 flex items-center justify-center" aria-label="Abrir menú">
                            <span class="material-symbols-outlined">menu</span>
                        </button>
                    </div>

                    <div class="flex items-center gap-1 sm:gap-2 flex-shrink-0">
                        {# Racha y XP (Solo para usuarios normales) #}
                        {% if not is_granted('ROLE_ADMIN') %}
                        <div class="flex items-center gap-1 bg-orange-500/10 text-orange-500 px-2 py-1 rounded-full border border-orange-500/20 text-[8px] sm:text-[10px] font-bold uppercase">
                            <span class="material-symbols-outlined text-xs sm:text-sm">local_fire_department</span>
                            <span class="hidden sm:inline">ACTIVO</span>
                        </div>

                        {# Barra de Nivel (oculta en móviles) #}
                        <div class="hidden lg:flex items-center gap-2">
                            <div class="text-right leading-none">
                                <p class="text-[8px] font-bold text-slate-500 uppercase tracking-widest">Nivel {{ app.user.level }}</p>
                                <p class="text-[10px] font-black text-primary italic uppercase">{% if app.user.level < 5 %}NOVATO{% elseif app.user.level < 15 %}INTERMEDIO{% else %}AVANZADO{% endif %}</p>
                            </div>
                            {% set xp_needed = (app.user.level + 1) * 100 %}
                            {% set xp_progress = app.user.pointsXp % xp_needed %}
                            {% set percentage = (xp_progress / xp_needed) * 100 %}
                            <div class="w-20 h-1.5 bg-slate-800 rounded-full overflow-hidden">
                                <div class="bg-primary h-full shadow-[0_0_8px_rgba(13,89,242,0.6)] transition-all duration-1000" style="width: {{ percentage }}%;"></div>
                            </div>
                            <span class="text-[8px] font-bold text-slate-400">{{ app.user.pointsXp }} XP</span>
                        </div>
                        {% endif %}

                        {# Perfil y Logout #}
                        <a href="{{ path('profile_index') }}" class="p-1.5 sm:p-2 rounded-lg bg-slate-800/40 border border-slate-700/50 text-slate-400 hover:text-primary transition-all flex items-center" title="Mi Perfil">
                            <span class="material-symbols-outlined text-sm sm:text-lg">person</span>
                        </a>
                        <a href="{{ path('app_logout') }}" class="p-1.5 sm:p-2 rounded-lg bg-slate-800/40 border border-slate-700/50 text-slate-400 hover:text-red-400 transition-all flex items-center" title="Cerrar Sesión">
                            <span class="material-symbols-outlined text-sm sm:text-lg">logout</span>
                        </a>
                    </div>
                </div>
            </header>
        {% endif %}

        {# Bloque Body Principal #}
        <main class="flex-1 overflow-y-auto scrollbar-hide">
            {# MENSAJES FLASH GLOBALES #}
            <div class="px-4 sm:px-6 pt-0 relative z-50">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="mb-4 p-4 rounded-xl font-bold text-sm flex items-center gap-2 {% if label == 'success' %}bg-green-500/10 border border-green-500/30 text-green-400{% else %}bg-red-500/10 border border-red-500/30 text-red-400{% endif %} shadow-lg transition-all animate-fade-in-down">
                            <span class="material-symbols-outlined">
                                {{ label == 'success' ? 'check_circle' : 'error' }}
                            </span>
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>

            {% block body %}{% endblock %}
        </main>
    </div>
</div>

<script>
    function toggleSidebar() {
        const sidebar = document.getElementById('sidebar');
        const backdrop = document.getElementById('sidebar-backdrop');
        
        // Alternamos el estado del sidebar en el eje X
        sidebar.classList.toggle('-translate-x-full');
        
        // Manejamos la aparición/desaparición del fondo oscuro
        if (sidebar.classList.contains('-translate-x-full')) {
            backdrop.classList.remove('opacity-100');
            setTimeout(() => {
                backdrop.classList.add('hidden');
            }, 300); // 300ms coincide con la transición de Tailwind
        } else {
            backdrop.classList.remove('hidden');
            setTimeout(() => {
                backdrop.classList.add('opacity-100');
            }, 10);
        }
    }

    // Cerrar el sidebar automáticamente al hacer click en cualquier link dentro del nav
    document.addEventListener('DOMContentLoaded', function() {
        const navLinks = document.querySelectorAll('nav a');
        navLinks.forEach(link => {
            link.addEventListener('click', function(e) {
                // Solo cerrar el sidebar en dispositivos móviles
                if (window.innerWidth < 768) { // md breakpoint
                    const sidebar = document.getElementById('sidebar');
                    const backdrop = document.getElementById('sidebar-backdrop');
                    
                    // Cierra el sidebar con la animación
                    if (!sidebar.classList.contains('-translate-x-full')) {
                        sidebar.classList.add('-translate-x-full');
                        backdrop.classList.remove('opacity-100');
                        setTimeout(() => {
                            backdrop.classList.add('hidden');
                        }, 300);
                    }
                }
            });
        });
    });
</script>
</body>
</html>
