<nav class="fixed top-0 left-0 right-0 z-50 bg-[#0d1117]/80 backdrop-blur-md border-b border-slate-800/50 h-20 flex items-center px-8">
    <div class="flex items-center justify-between w-full">
        {# Logo #}
        <a href="{{ path('app_home') }}" class="flex items-center gap-3 group">
            <h1 class="text-2xl font-black italic uppercase text-white tracking-tighter">
                META<span class="text-primary">FIT</span> AI
            </h1>
            <div class="bg-primary p-1.5 rounded-lg shadow-[0_0_15px_rgba(13,166,242,0.4)]">
                <span class="material-symbols-outlined text-white text-xl">bolt</span>
            </div>
        </a>

        {# Enlaces y Logout #}
        <div class="flex items-center gap-8 text-sm font-bold uppercase tracking-widest">
            <a href="{{ path('app_home') }}" class="text-white hover:text-primary transition-colors">Dashboard</a>

            <div class="h-6 w-[1px] bg-slate-800 mx-2"></div>

            <div class="flex items-center gap-4">
                <span class="text-slate-500 text-xs">{{ app.user.name }}</span>
                <a href="{{ path('app_logout') }}" class="text-slate-400 hover:text-red-400 transition-colors">
                    <span class="material-symbols-outlined">logout</span>
                </a>
            </div>
        </div>
    </div>
</nav>
